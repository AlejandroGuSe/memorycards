import React from 'react';
import { Button } from 'reactstrap';

function Home() {
    return (
        <div>
             <Button
                color="primary"
                href="#"
                tag="a"
            >
                Link
            </Button>
        </div>
    );
}

export default Home;
